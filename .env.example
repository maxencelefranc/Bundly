# Rename to .env and fill with your values. Either set the private names
# (read at build-time via app.config.ts) OR the EXPO_PUBLIC_ names (available in app code).

# Option A — private vars consumed by app.config.ts -> extra
SUPABASE_URL=https://YOUR-PROJECT.supabase.co
SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY

# Option B — public runtime vars (also supported)
EXPO_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY
